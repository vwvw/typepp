diff --color -ruN src/dataset.h src_mod/dataset.h
--- src/dataset.h	2024-01-19 00:29:09.450809471 +0100
+++ src_mod/dataset.h	2024-01-29 23:05:21.031076091 +0100
@@ -189,7 +189,7 @@
       theitem[newkey.idx].info = thenum;
       ++thenum;
 
-      return &(theitem[newkey.idx].data);
+      return (DATA*) &((theitem[newkey.idx]).data);
    }
    /// creates new (uninitialized) data element in #DataSet.
    /**@return Pointer to the newly created element.
